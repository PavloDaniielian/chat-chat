import React, { Component } from 'react';
import ChatPage from './modules/chat-page';

class App extends Component {
  render() {      
    return (
      <div>
        <ChatPage/>
      </div>
    );
  }
}

export default (App);